# SMP Spectator Return

A lightweight server-side Fabric mod that lets players enter Spectator mode with `/s` and return to their saved position by using the command again.

## Main features

- Server-side only
- Persistent return positions
- Restores the previous game mode
- Creative and Adventure restrictions
- Per-world allow list
- Configuration reload with `/s reload`
